#include <a_samp>

#define _DEBUG 2
#include <tdw_debug>

main()
{
	p:0("Debug level is zero");
	p:1("Debug level is greater than zero");
	p:2("Debug level is greater than one");
	p:3("Debug level is greater than two");
}
